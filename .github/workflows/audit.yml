# Calls the family reusable audit.
jobs:
  audit:
    permissions:
      contents: read
      packages: read
    uses: OlivierZal/configs/.github/workflows/reusable-audit.yml@c498c9308d041f8df580293edd0f39df255198e0 # v1.2.0
name: Audit
on:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch: {}
permissions: {}
