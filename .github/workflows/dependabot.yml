# Calls the family workflow of the same name in OlivierZal/configs.
jobs:
  dependabot:
    permissions:
      contents: write
      pull-requests: write
    uses: OlivierZal/configs/.github/workflows/dependabot.yml@a2ee75bd9cd4b8062b993e2ea488883c2863a2c5 # v6.2.0
name: dependabot
on:
  pull_request: {}
permissions: {}
