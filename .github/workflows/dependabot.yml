# Calls the family workflow of the same name in OlivierZal/configs.
jobs:
  dependabot:
    permissions:
      contents: write
      pull-requests: write
    uses: OlivierZal/configs/.github/workflows/dependabot.yml@9b69e8520dfd11484df292ca75c10d51beba75ca # v6.3.1
name: dependabot
on:
  pull_request: {}
permissions: {}
