concurrency:
  cancel-in-progress: false
  group: publish
jobs:
  publish:
    environment: homey
    name: Publish app on Homey
    permissions:
      contents: read
      packages: read
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
      - uses: ./.github/actions/setup-node-and-install
        with:
          cache: none
          npm-token: ${{ secrets.GITHUB_TOKEN }}
      - run: cp "$NPM_CONFIG_USERCONFIG" "$GITHUB_WORKSPACE/.npmrc"
      # The webview bundles are gitignored esbuild outputs: without this
      # step the packed app ships settings/widget pages whose script src
      # 404s on every store install ("Loading failed" — the #1404 root
      # cause; dev-installs mask it because the working tree holds local
      # builds).
      - name: Build webview bundles
        run: npm run build:assets
      - name: Assert the bundles are in the package
        run: |
          for file in settings/index.js settings/index.mjs \
            widgets/ata-group-setting/public/index.js \
            widgets/ata-group-setting/public/index.mjs \
            widgets/charts/public/index.js widgets/charts/public/index.mjs; do
            if [ ! -s "$file" ]; then
              echo "::error::Missing webview bundle: $file"
              exit 1
            fi
          done
      - id: publish
        name: Publish
        uses: athombv/github-action-homey-app-publish@0642b483f1eb66fbceb0c91b73df35d45fd2f3db
        with:
          personal_access_token: ${{ secrets.HOMEY_PAT }}
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: URL
        env:
          URL: ${{ steps.publish.outputs.url }}
        run: echo "Manage your app at $URL" >> "$GITHUB_STEP_SUMMARY"
    timeout-minutes: 30
name: Publish app on Homey
on:
  release:
    types: [published]
  workflow_dispatch: {}
permissions: {}
